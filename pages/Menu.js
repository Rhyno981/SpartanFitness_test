import React from "react";
import {StyleSheet, Text, View, ScrollView} from 'react-native';

const Menu = (props) => {
  return (
    <View>
        <Text>Menu</Text>
    </View>
  )
};

export default Menu;
